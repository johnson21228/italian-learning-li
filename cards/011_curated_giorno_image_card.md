# Card 011 — Curated Giorno Image

The first curated site image is for `giorno`.

The image is stored at:

- `site/images/vocabulary/curated/giorno.png`

The vocabulary entry references this stable site path and is marked curated.

Future image edits may replace the PNG at the same path without changing the website data contract.
